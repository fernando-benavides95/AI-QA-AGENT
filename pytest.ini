[pytest]
testpaths = test
markers =
    unit: deterministic, no LLM calls — safe for CI
    llm: requires real LLM API call — run locally or in eval pipeline
filterwarnings =
    ignore::pytest.PytestCollectionWarning
